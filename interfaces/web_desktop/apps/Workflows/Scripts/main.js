Application.run = function( msg )
{
	
}

// Workflow canvas UI engine
WorkflowCanvas = {
	canvas: ge( 'MainCanvas' ),
	drawInfo: {
		zoom: 1
	},
	elements: [],
	refresh()
	{
		for( var a = 0; a < elements.length; a++ )
		{
			
		}
	},
	drawElement( px, py )
	{
	}
};
